# ═══════════════════════════════════════════════════════════════
# DEPRECATED — retired 2026-09-07
#
# postgres / mariadb / redis / minio moved to a centralized DB host:
#   nodes/anno-db-oci-01/  (OCI VPS, reachable over Tailscale)
#
# See docs/guides/db-migration-to-oci.md for the migration.
#
# The core-data_default network this stack used to own has been retired
# too — n8n/, twenty-personal-crm/, and peekaping/ now join the existing
# `annogrid` external network (created by ../application-server/setup.sh) instead, since they
# no longer need to share a Docker network with the DB containers to reach
# a remote host over Tailscale.
#
# Kept as an intentional no-op (rather than deleted) for history/rollback
# reference.
# ═══════════════════════════════════════════════════════════════
name: core-data
services: {}
