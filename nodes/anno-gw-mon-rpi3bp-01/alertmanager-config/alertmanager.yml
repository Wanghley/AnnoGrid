global:
  resolve_timeout: 5m

route:
  receiver: default
  group_by: ['alertname', 'instance']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: default
    # TODO: wire up a real receiver before relying on this for anything.
    # Examples:
    # webhook_configs:
    #   - url: https://ntfy.sh/your-topic-here
    # slack_configs:
    #   - api_url: https://hooks.slack.com/services/XXX/YYY/ZZZ
    #     channel: '#annogrid-alerts'
